import { component$ } from '@builder.io/qwik';
import WebsitesSection from '../../components/how-it-works/WebsitesSection';

export default component$(() => {
  return (
    <div class="w-full">
      <WebsitesSection />
    </div>
  );
}); 